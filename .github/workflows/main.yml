name: CI
on:
  branches:
    - main
    - release/*
on:
  workflow_dispatch:
jobs:
  CI:
    runs-on: ubuntu-22.04
    
    steps: 
      -name : Setup do Node JS
      uses: actions/setup-node@v4
      with:
        node-version: 16.13.2
        
      
     
